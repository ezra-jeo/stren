# One-shot prompt — Codex 5.6 Sol — Unified Accounts backend (Agent C, units C1–C3)

_Packaged from `AgentsContextKnowledgeBase/ImplementationPlan-UnifiedAccounts.md` (the plan wins on divergence). Recommended launch: **Codex 5.6 Sol, reasoning effort xhigh** — auth/RLS restructure; errors here are silent security holes. Paste everything below the line._

---

You are rebuilding Stren's identity model and auth flows: **one account for all of Stren**, gyms attached through a new `gym_users` table, a server-side active gym, and auth routes rebuilt from scratch with the old code deleted. You are Agent C (backend/logic). You ship first; Claude Opus 4.8 (Agent U) will reskin your minimal pages afterward. Every design decision is already made — execute the spec.

**Grill-session amendments (2026-07-11) — already folded into the plan, binding:** managers are members too (`/member`, kiosk, streaks, leaderboard work for ANY active affiliation — plan §2.1); the kiosk **pins its gym at launch** and every kiosk RPC takes explicit `p_gym_id` validated affiliation-based, never `get_gym_id()` equality (§2.2); all self-joins land **pending**, join-by-code works for unpublished gyms, `create_gym` gets slug/reserved-code/3-unpublished-gyms-cap guards (§2.5); lapsed-subscription members get a locked member portal — `member_home_stats` gains `subscription_status` + `lapsed_summary`, leaderboard excludes lapsed (§2.6); attribution + owner alerts — `payments.recorded_by`, `memberships.created_by`, `gym_users.added_by`, and an owner in-app notification per recorded payment (§2.7). Phone-OTP, org/multi-branch, and email digests are explicitly deferred (§0) — build nothing for them.

**Read first, in order:**
1. `AGENTS.md` → `AgentsContextKnowledgeBase/Catalog.md` — the documentation system and your update obligations.
2. `AgentsContextKnowledgeBase/ImplementationPlan-UnifiedAccounts.md` — **the contract.** §0–§4 and §6–§8 are your spec; §1 is a verified inventory, trust it.
3. `AgentsContextKnowledgeBase/ImplementationState.md` — your units are C1–C3.
4. `CLAUDE.md` — conventions (migrations, test-first, money-path rule, branching; **never commit/push — leave everything in the working tree**).
5. `docs/adr/0004-one-account-many-gyms.md` — why, and which alternatives were rejected.
6. Migrations `011`, `015`, `016` and `lib/access-data.ts` — the helper conventions and the `get_my_access()` shape you must preserve exactly.

**You own (create/edit):** `supabase/migrations/019_unified_accounts.sql` (plan §3, in its exact order: `gym_users` + `active_gym_id` → backfill → helper re-implementations (`get_gym_id`, `get_user_role`, `is_manager`, `has_gym_permission`, `get_my_access`, `validate_permission_override`, `handle_new_user`) → new RPCs (`get_my_gyms`, `set_active_gym`, `join_gym`, `create_gym`) → drops (`check_gym_membership`, `create_gym_and_owner`) → per-gym unique fixes incl. every RPC `ON CONFLICT` target → function sweep via `grep -n "public.profiles" supabase/migrations/*.sql` → column drops, which are **pre-approved by plan §0.5**); regenerated `lib/database.types.ts`; `middleware.ts` rewrite (§4: static 308 map from §2.3, one `get_my_access` for all protected surfaces, authed-no-gym → `/gyms`, delete all login-origin machinery); `lib/auth-actions.ts` (§6.3 signatures); `lib/auth-context.tsx` rebuild to the §6.2 interface (keep the hardened session lifecycle — hash tokens, refresh-token recovery, lock retry, password-setup flags — delete origin/sign-out-routing/role-redirect logic); `app/api/admin/members/onboard` attach-vs-create rework (§2.5); `lib/types.ts` (`AccountProfile`, `MyGym`, etc.); the app-side profiles-query sweep (§4 — let `tsc` find consumers of the dropped columns); minimal functional `/login`, `/signup`, `/gyms`, `/gyms/new` pages marked `TODO(U)`; `app/auth/callback` + `/reset-password` post-auth re-pointing to `resolvePostAuthDestination`; the cache-key audit per `docs/CACHING.md`; unit C3's deletion sweep (§1 "Delete" rows + `lib/login-origin.ts`, `lib/sign-out-routing.ts`, `components/auth/LoginForm.tsx`, `components/auth/GymSignUpForm.tsx`, their tests, grep-zero per DoD §10.5); E2E auth helper/spec rewrite to `/login`; quarantine `OTP-AUTH-GUIDE.md` in the Catalog Stale table.

**You must NOT touch:** `components/admin/gym-studio/*`, `GymLandingPreview.tsx`, `AccessClient.tsx`, `lib/permissions.ts`, `lib/features.ts`, `lib/access.ts`, `lib/access-data.ts`, `lib/access-context.tsx`, shell/nav visual JSX (`app/admin/layout.tsx` chrome, `MemberShell.tsx` — Opus adds the switcher), or the billing `memberships`/`payments` schemas (only their policies' helper internals change).

**Order of work (test-first — write each §7 C-row test red before its implementation):**
1. **C1** — migration 019 + regenerated types + integration tests: `gym-users-access` (multi-gym matrix incl. fail-closed NULL/stale active gym and pending/rejected granting nothing), `set-active-gym`, `join-and-approve` (incl. last-owner protection), plus keeping `payments-access.characterization` and the 015/016 permission/feature suites green untouched. RLS-recursion rule: `gym_users` policies never subquery `gym_users` — SECURITY DEFINER helpers only, migration-011 conventions (`SET search_path = ''`, explicit GRANT/REVOKE).
2. **C2** — middleware + `lib/auth-actions.ts` + auth-context rebuild + onboard rework + query/type sweep + minimal pages + cache audit, with `post-auth-destination` (all five §2.4 rules), `onboard-existing-account`, middleware redirect-map, and auth-action tests.
3. **C3** — deletion sweep (grep-zero), E2E rewrite, docs (`ImplementationState.md` rows C1–C3, `CHANGELOG.md` under Unreleased, `OTP-AUTH-GUIDE.md` quarantine). Version bump to 2.0.0 happens at J1, not here.

**Rules:** migrations idempotent, backfill strictly before drops, never the dashboard; `maybeSingle()` for maybe-empty reads; UI hiding is never the only enforcement — RLS/RPC first, API 403 second, middleware redirect third; delete old code, do not deprecate; work on branch `auth/unified-accounts`; no commits — leave changes in the working tree and report.

**Definition of done (your half):** plan §10 items 1–7 provable locally; `npm run lint && npm run typecheck && npm run test:unit && npm run build` green and E2E green where credentials allow; `ImplementationState.md` + `CHANGELOG.md` updated in the same working tree. List anything deliberately skipped and every place you had to interpret the spec in your final summary.
