# One-shot prompt — Codex/GPT backend agent — logic half (Agent B, units B0–B6)

_Packaged from `AgentsContextKnowledgeBase/ImplementationPlan.md` §10.B (the plan wins on divergence), updated after Fable's 2026-07-11 review of the completed Agent A UI half. Recommended launch effort: **extra high** (security-critical SQL; errors here are silent). Paste everything below the line into the agent._

---

You are implementing the **entire logic/backend half** of the "Gym Page Studio + Permissions & Feature Toggles" workstream for Stren, in one pass. You are Agent B. The UI half (Agent A, Claude Opus 4.8) is already implemented on branch `CustomizationPermissionsToggles` and reviewed — you are building the SQL/server surface it codes against. Every design decision is already made; execute the spec.

**Read first, in order:**
1. `AGENTS.md` → `AgentsContextKnowledgeBase/Catalog.md` — the documentation system and your update obligations.
2. `AgentsContextKnowledgeBase/ImplementationState.md` — units A1–A5 are shipped; A6 (UI fix pass) may land in parallel; your units are B0–B6.
3. `AgentsContextKnowledgeBase/ImplementationPlan.md` — **the contract.** §0 (sequencing + security findings), §2–§6 (architecture, matrices, migrations, hardening) and §8 (frozen contracts), §9 (your tests, rows marked B) are your spec.
4. `CLAUDE.md` — conventions (migrations, test-first, money-path rule, branching). They bind you.
5. `PHASE_3_TO_7_DIAGNOSTIC_AND_PLAN.md` — detail for the Slice 0 security fixes.
6. The shipped contract modules — `lib/permissions.ts`, `lib/features.ts`, `lib/access.ts`, `lib/access-data.ts` — and `tests/fixtures/role-permission-defaults.json`. **These are frozen; consume, never modify.** Your SQL must match them exactly: seed `gym_role_permission_defaults` from the fixture; `get_my_access()` must return `{ role, gym_id, permissions: text[], features: jsonb }` exactly as `lib/access-data.ts` coerces it; `gym_feature_settings` and `gym_user_permission_overrides` must match the column names `lib/access-data.ts` reads/writes (`flags`, `updated_by`, `updated_at`; `gym_id, user_id, permission, granted, granted_by, updated_at`, PK `(gym_id,user_id,permission)`).

**You own (create/edit):** `supabase/migrations/014…017` per plan §5; `lib/permissions-server.ts` (§8.5); `middleware.ts` permission map (§6); `app/api/admin/*` + `app/api/member/avatar` hardening (§6); server guards/gates in `app/admin/page.tsx`, `app/admin/reports/page.tsx`, **`app/admin/access/page.tsx` (`requirePermission('roles:manage')`)**, `app/member/layout.tsx`, `app/member/feed/page.tsx`, `app/member/leaderboard/page.tsx`, public subpage `notFound()` gates, `app/gym/[code]/layout.tsx` feature-prop wiring into `GymTopNav`; `app/member/page.tsx` (drop the manager-only `kiosk_get_checked_in` call; use the new `people_in_gym` field you add to `member_home_stats`); `lib/engagement-hooks.ts` feature check; the kiosk "Check-ins are turned off" screen; the announcements admin page (smoke-test it — the nav item is already live behind `announcements` + `announcements:manage` — **fix the page if it's broken**, user-confirmed in scope); finance-optional typing in `AdminDashboardClient.tsx`/`AdminReportsClient.tsx` (types + a quiet "—" fallback, no visual redesign); regenerate `lib/database.types.ts` after each migration.

**You must NOT touch:** `components/admin/gym-studio/*`, `components/gym/GymLandingPreview.tsx`, `components/admin/AccessClient.tsx`, visual JSX of nav/shell components, the five contract modules above, `lib/brand-color.ts`, `lib/focal.ts`, or the media-pipeline handlers inside `GymPageStudio.tsx`.

**Order of work (test-first; money-path characterization BEFORE 015):**
1. **B0 — Slice 0 security hotfix, own PR to `qa` immediately:** migration `014_fix_notification_rpc_scope.sql` per §5 (revoke `process_daily_notifications` from authenticated; caller checks on `create_member_notification`, `kiosk_update_streak`, `can_send_member_notification`) + gym-scope check in `app/api/admin/revalidate-gym/route.ts` + `avatarUrl` origin validation in `app/api/member/avatar/route.ts` + RPC probe regression tests. Update the Phase 2.6 row in `CLAUDE.md` on merge.
2. **B1 — migration `017_gym_cover_focal_and_sections.sql` EARLY** (yes, before 015/016; numbering is not ordering — keep filenames as planned or renumber coherently, your call, but ship the columns first): `gyms.cover_focal` + `gyms.section_visibility` per §5. **This is urgent**: until these columns exist, the shipped Studio degrades (its pre-017 load fallback and save guard — see `prompts/Opus48-UI-FixPass.md` Fix 1).
3. **B2 — `payments-access.characterization.test.ts`** pinning current per-role payment read/insert behavior (CLAUDE.md money-path rule). Must be green before 015.
4. **B3 — migration `015_permission_model.sql`** per §5: defaults + overrides tables (seed owner row for EVERY key from the fixture — canonical registry), `has_gym_permission` (owner short-circuit; **RAISE on unknown key for non-owners**; edit ⊇ view), interim `get_my_access`, RLS for the new tables + the never-delegable trigger (`roles:manage`/`features:manage`/`gym_page:publish`, no owner/member targets), policy swaps (`gyms_update` → `gym_page:edit`; `protect_gym_publish` trigger; `payments` incl. replacing `dev_all_payments`; plans/promos/announcements → their keys), `admin_dashboard_stats`/`admin_reports_data` gating with the **exact finance field lists in §5 — do not re-derive them**.
5. **B4 — migration `016_feature_toggles.sql`** per §5: `gym_feature_settings` + RLS, `gym_feature_enabled`, `get_gym_by_code` rework (public feature flags; omit disabled-feature data; include `cover_focal`/`section_visibility`/`logo_path`/`cover_path`; **the `is_published` bugfix requires explicit user sign-off before you ship it — ask, do not assume**), leaderboard empty-when-off, kiosk feature checks, feed/announcements/promos policy additions, final `get_my_access` (features via `gym_feature_enabled`), `people_in_gym` in `member_home_stats`.
6. **B5 — server enforcement wiring** per §6: middleware `ROUTE_PERMISSIONS` map via one `get_my_access` call (keep `x-gym-id`/`x-user-role` forwarding), `requirePermission` on server pages, `apiRequirePermission` + gym scope on `cache-health`/`members/onboard`, member layout features prop, feed/leaderboard redirects, public subpage 404s, `GymTopNav` wiring, kiosk-off screen, engagement-hooks feature check (check-in must survive an RLS-rejected feed insert), announcements page verify/fix, plus the §9 B-marked integration/E2E tests (E2E account strategy per §9 note — decide on inspection).
7. **B6 — LAST: convert `app/admin/gym-profile/page.tsx`** to the thin server wrapper: `requirePermission('gym_page:view')`, fetch gym row + access + feature flags server-side, render `<GymPageStudio initialGym={…} access={…} initialFeatureFlags={…}/>` (the component already accepts these optional props).

**Rules:** migrations idempotent, no destructive SQL, never the dashboard; new SQL helpers follow `get_gym_id()` conventions (`SECURITY DEFINER`, `SET search_path = ''`, explicit GRANT/REVOKE — pattern reference: migration 011); UI hiding is never the only enforcement — RLS/RPC first, then API 403, then middleware redirect; `maybeSingle()` for maybe-empty reads; production rollout per slice = migrations first, deploy immediately after. Bump `package.json` + `CHANGELOG.md` per shipped slice group.

**Definition of done:** full `npm run test:ci` green; the plan §10 "Definition of done" walked end-to-end (admin blocked from Studio/publish/toggles/roles by URL+RPC+REST, finance visible by default and revocable per switch, member loses disabled features everywhere, public 404s + payload omission, kiosk-off screen, RPC probes fail for members); update `AgentsContextKnowledgeBase/ImplementationState.md` rows B0–B6 + J1 and `CHANGELOG.md` in the same PR. List anything deliberately skipped, and every place you had to interpret the spec, in your final summary.
