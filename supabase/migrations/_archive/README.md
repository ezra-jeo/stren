# Migration Archive

These draft migrations (001–004) were the original incremental setup scripts.
They are superseded by `../001_production_baseline.sql`, which is a single
idempotent file that reproduces the full production schema.

Do **not** apply these files — use the baseline instead.
